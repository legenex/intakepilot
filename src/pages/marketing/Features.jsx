import React from 'react';
import StubPage from './StubPage';
export default function Features() {
  return <StubPage title="Features" subtitle="Deep-dive into every part of the AI intake stack." hint="8 feature sections with side-by-side visuals, anchor-linkable from the home grid." />;
}