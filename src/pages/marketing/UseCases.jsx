import React from 'react';
import StubPage from './StubPage';
export default function UseCases() {
  return <StubPage title="Use Cases" subtitle="Built for three types of operators." hint="Index page with 3 large cards linking into PI Firms, Lead-Gen Agencies, and Aggregators." />;
}